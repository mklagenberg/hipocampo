# V3 pre-injection privacy gate

X4.4 applies a privacy gate before retrieval, compilation, injection, export,
cache, embedding, telemetry and external processing. The gate evaluates
identity, destination, purpose, boundary evaluability and minimization.

Secrets, revealing metadata, unauthorized derived restrictions and external
processing without an authorized environment are blocked. The gate reports
policy metadata, not protected content.
