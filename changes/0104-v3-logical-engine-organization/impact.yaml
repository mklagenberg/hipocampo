change_set:
  id: "0104-v3-logical-engine-organization"
  class: "normative"
  semver: "minor"
  status: "implemented"
  backfill: false
  decisions:
    - "decisions/0104-v3-logical-engine-organization.md"
impact:
  - artifact: "docs/engines/catalog.md, docs/engines/dependency-map.md, docs/engines/test-matrix.yaml"
    status: "updated"
    note: "Defines the logical engine catalog, dependencies and deterministic/semantic test assignment."
  - artifact: "scripts/validate_v3_engine_catalog.py, scripts/validate_v3_engine_suite.py"
    status: "updated"
    note: "Validates engine count, contracts, modules, commands and test-case completeness, then executes the deterministic matrix and checks semantic fixture references."
  - artifact: "decisions/0104-v3-logical-engine-organization.md, CHANGELOG.md"
    status: "updated"
    note: "Records the logical organization and its non-refactoring boundary."
validation:
  commands:
    - "python scripts/validate_v3_engine_catalog.py --root ."
    - "python scripts/validate_hipocampo.py --root ."
    - "python scripts/validate_change.py --root ."
    - "python scripts/validate_contracts.py --root ."
    - "python scripts/validate_v3_crud_use_cases.py --root ."
    - "python scripts/validate_semantic_fixtures.py --root ."
  evidence:
    - "docs/engines/catalog.md"
    - "docs/engines/dependency-map.md"
    - "docs/engines/test-matrix.yaml"
    - "decisions/0104-v3-logical-engine-organization.md"
  notes: "Logical organization only; no physical source refactor, V3 activation, vault migration, publication or external MCP write."
