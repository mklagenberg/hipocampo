# Change Set 0066 — Contract drift routing

## Intent

Make coordinated updates explicit and route drift through compatibility checks.

## Decision

Unknown, incompatible and unavailable drift blocks the affected operation;
content-only changes do not create a false contract migration.
