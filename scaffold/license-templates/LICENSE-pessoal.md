ALL RIGHTS RESERVED — PRIVATE PROPRIETARY KNOWLEDGE

Copyright (c) [YEAR] [FULL NAME] (@[github-username]). All rights reserved.

This repository (`[repository-name]`) is a private Hipocampo content vault. Its
content and frontmatter belong exclusively to [FULL NAME]. It is not an open-source
or public-content repository, even when a document is classified as
`visibility: public`.

1. OWNERSHIP

   All content in this repository is the exclusive property of [FULL NAME]. The
   `author` and `contributors` fields record credit and attribution only; they do
   not transfer co-ownership or a usage right. An AI agent acting under the holder's
   direction is an operational tool, not an author or rights holder.

2. PRIVATE ACCESS AND VISIBILITY

   The repository remains private. `visibility` and its mechanically derived
   `LicenseRef-<idstring>` are handling classifications for people already authorized
   to access this private repository. In particular, `visibility: public` means no
   additional handling restriction within that authorized context; it does not grant
   an open license, external publication, copying, redistribution, sublicensing, or
   any right to a person merely because they have repository access.

3. CURATION BOUNDARY

   A `content`-level vault may use the four handling classifications
   `public`/`internal`/`confidential`/`restricted`. A `vault`-level repository uses
   only `confidential`/`restricted` for new or touched content. The selected profile
   declares the curation level in `hipocampo.yaml`; it never changes this license's
   private/proprietary character.

4. AGENT OBLIGATIONS

   Before processing a document, an AI system reads its `visibility` and `license`
   fields and applies the corresponding handling limit. It never uses confidential or
   restricted content in a lower-privilege context, and it refuses a request that
   would expose or synthesize content beyond its allowed context.

5. NO REDISTRIBUTION

   No person or system may copy, reproduce, publish, sublicense, disclose, index in
   an external service, or use this content for training or fine-tuning without the
   holder's prior, specific, written authorization. Such authorization must state its
   scope, purpose, and recipient. Silence, repository access, or `visibility: public`
   is not authorization.

THIS MATERIAL IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
IMPLIED. IN NO EVENT SHALL THE HOLDER BE LIABLE FOR DAMAGES OR CLAIMS ARISING FROM
UNAUTHORIZED USE OF THIS MATERIAL.
